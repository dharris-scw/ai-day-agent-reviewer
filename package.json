{
  "name": "agent-review",
  "version": "0.1.0",
  "description": "Autonomous GitHub PR review CLI",
  "type": "module",
  "bin": {
    "agent-review": "./dist/src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
